Put your exported GLB models here:
  - earth.glb
  - mars.glb
  - jupiter.glb
  - moon.glb

Export from Blender via:  File → Export → glTF 2.0 (.glb)

Then enable them by uncommenting the `glb:` lines in js/main.js
in each CATALOGUE entry.
